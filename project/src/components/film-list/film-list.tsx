import { useState } from 'react';
// import Card from '../card/card';
import { Films } from '../../types/types';
export{};

// export default function FilmList({films} : { films: Films []}) {
// //     // const {films}=films1;
//     console.log(films);
//   const [activeCardId, setActiveCardId] = useState('test-ID');
//   const makeActive = (id) => setActiveCardId(id);
//   return (
//     <div>
//       {films.map((film) => {
//         if (film.id === activeCardId) {
//           return (
//             <Card id={film.id} makeActive={makeActive} isActive={true} />
//           );
//         } else {
//           return (
//             <Card id={film.id} makeActive={makeActive} isActive={false} />
//           );
//         }
//       })}
//     </div>
//   );
// }

// function Card(id, makeActve, isActive) {
//   return (
//     <span
//       className={'class?' + isActive ? 'active class' : ''}
//       onMouseOver={() => makeActve(id)}
//     >
   
//       {id}
//     </span>
//   );
// }
